hfig = figure;
imshow(scaled(rand(40,40));
h = imellipse;

% image segmentation: threshold
% adaptive thresholding
% sensitivity adjusting




% multi-level thresholding?





function keypress(~, evnt)
        
        
        switch lower(evnt.Key)
            case 'rightarrow'
                data.i = min(data.i + 1, data.imax); 
            case 'leftarrow'
                data.i = max(1, data.i - 1);
     
            otherwise
                return;
        end

        
    end





function testroi(pos)

pos

end